# Vizinhando Frontend - Produção
# Este arquivo será usado automaticamente no deployment

# URL do backend (será configurada automaticamente pelo Emergent)
REACT_APP_BACKEND_URL=${REACT_APP_BACKEND_URL}
WDS_SOCKET_PORT=443

# Build configuration
GENERATE_SOURCEMAP=false
REACT_APP_ENV=production
